#include"main.h"

/**
 * mul - multiplicate two integers
 * @a: firn number to multiplicate
 * @b: second number to multiplicate with the first number
 * Return: the mutliplication between x para and y para
 */

int mul(int a, int b)
{
	return (a * b);
}
